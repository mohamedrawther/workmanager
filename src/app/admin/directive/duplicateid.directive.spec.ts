import { DuplicateidDirective } from './duplicateid.directive';

describe('DuplicateidDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateidDirective();
    expect(directive).toBeTruthy();
  });
});
